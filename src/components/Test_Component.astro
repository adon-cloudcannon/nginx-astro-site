---

interface Props {
	/** The code to highlight. Required. */
	code: string;
}

const {
	code,
	...rest
} = Astro.props;

const innerContent = await Astro.slots.render("default")
import { parse_marked } from "@utils/nxt";

const parsed = parse_marked("```bash\n"+innerContent+"\n```")
console.log(innerContent)
console.log(parsed)
//console.log(code)


const html = `<pre><code>${code}</code></pre>`
---

<Fragment set:html={parsed} />