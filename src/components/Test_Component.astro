---
const props = Astro.props;
const hints = props.hints;
let content = await Astro.slots.render('default')
console.log(content)
if(hints)
{
    hints.forEach(hint => {
        let content_string = `:nxt_hint: \`${hint.display_text} <${hint.tooltip}>\``;
        console.log(content_string)
    })
}
---

Hello world.