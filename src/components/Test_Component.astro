---
import { nxt_hint } from "@utils/nxt";

let innerContent = await Astro.slots.render("default")
innerContent = innerContent.replaceAll("&quot;",`"`)
							.replaceAll("&#39;","'")
							.replaceAll("&lt;","<")
							.replaceAll("&gt;",">")
const html = nxt_hint("```bash\n"+innerContent+"\n```")

---

<Fragment set:html={html} />